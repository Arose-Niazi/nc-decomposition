name: nc-decomposition

services:
  web:
    build: .
    container_name: nc-decomposition-web
    restart: unless-stopped
    networks:
      - proxy
    expose:
      - "80"

networks:
  proxy:
    external: true
